var imgElements = document.querySelectorAll('.img-brg');

gsap.from(imgElements.item(0), {
    duration: 1,
    y: -200, // Delay each image slightly
});

gsap.from(imgElements.item(1), {
    duration: 1,
    y: -150, // Delay each image slightly
});

gsap.from(imgElements.item(2), {
    duration: 1,
    y: -100, // Delay each image slightly
});

gsap.from(imgElements.item(3), {
    duration: 1,
    y: -50, // Delay each image slightly
});

gsap.from(imgElements.item(4), {
    duration: 1,
    y: 50, // Delay each image slightly
});

gsap.from(imgElements.item(5), {
    duration: 1,
    y: 100, // Delay each image slightly
});

gsap.from(imgElements.item(6), {
    duration: 1,
    y: 150, // Delay each image slightly
});